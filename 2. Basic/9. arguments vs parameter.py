# parameters are the names used when defining a function or a method, and arguments are the values you pass to a function when calling it.
def say_hello(name):
    print(f'Hello, {name}!')

# arguments
say_hello('John') # Hello, John!